import { ApolloProvider, useQuery } from '@apollo/client';
import client from '@/libs/client';


const Home = () => {
  return (
    <div>
      <h1>Departamentos</h1>
    </div>
  );
};

export default Home;
